services:
  gru:
    build: .
    ports:
      - "${BIND_HOST:-0.0.0.0}:${HOST_PORT:-8080}:8080"
    volumes:
      - gru-data:/app/data
    environment:
      - DATA_DIR=/app/data
      - PORT=8080
      - APP_BASE=${APP_BASE:-/}
    restart: unless-stopped

volumes:
  gru-data:
